spring:
  application:
    name: meditrack-identity-service
  profiles:
    active: bd

  security:
    user:
      name: luis
      password: test

server:
  port: 8080

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
