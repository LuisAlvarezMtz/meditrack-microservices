spring:
  datasource:
    url: "jdbc:postgresql://localhost:5433/identity_db"
    username: "${DB_USERNAME}"
    password: "${DB_PASSWORD}"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
